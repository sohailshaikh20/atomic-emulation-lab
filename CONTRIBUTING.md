# Contributing

## Style
- PowerShell: follow PSScriptAnalyzer guidance.
- Keep scripts safe-by-default (prefer discovery techniques).

## Pull Requests
- Update docs in `docs/` for any new scripts or playbooks.
- Add at least one example run screenshot (redact sensitive info).
